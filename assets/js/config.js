/**
 * ENM Investor Portal — backend configuration.
 * Set Supabase credentials here to enable live account creation, KYC, and allocation tracking.
 */
window.ENM_PORTAL_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: '',
  previewMode: true,
};
